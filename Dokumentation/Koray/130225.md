# February 13th

* removed the line "await mongoose.connection.dropDatabase()", because obviously the database should NOT be dropped

* *MellowDreams* is the title of the app it is :)
  
* suggestions for cloud effects seen below

```
// /components/Cloud.js

import React from "react";

const Cloud = () => {
    return <div className="cloud"></div>
};

export default Cloud;

// to globals.css

@keyframes moveClouds {
  from {
    transform: translateX(-200px);
  }
  to {
    transform: translateX(100vw);
  }
}

.cloud {
  position: absolute;
  top: 20%;
  left: -200px;
  width: 200px;
  height: 100px;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 100px;
  box-shadow: 50px 25px 50px rgba(255, 255, 255, 0.5);
  animation: moveClouds 20s linear infinite;
}

// to /app/page.js

import Button from "./components/Button";
import Image from "next/image";
import Cloud from "./components/Cloud"; // Import the Cloud component

export default function Home() {
  return (
    <>
      {/* Cloud Effect */}
      <Cloud />
      <Cloud /> {/* Add more clouds if needed */}

      <div className="fixed left-0 top-1/3 md:top-1/2 -translate-y-1/2 block slide-in">
        <Image
          src="/kids/boy-peek.png"
          alt="Kids illustration"
          width={140}
          height={400}
          className="md:w-[240px] xl:w-[390px]"
        />
      </div>

      <div className="flex flex-col min-h-screen justify-between items-center text-center">
        <div className="w-full px-4 sm:px-0">
          <Image
            src="/Kinderbuch.svg"
            alt="Header illustration"
            width={350}
            height={200}
            priority
            className="m-8 w-[280px] sm:w-[350px] h-auto mx-auto mb-16"
          />
        </div>

        <div className="text-3xl font-black max-w-[14rem] sm:max-w-none mx-auto">
          where dreams are made
        </div>

        <div className="font-black grid grid-cols-1 gap-4 justify-items-center mb-8">
          <Button variant="primary" href="/register">
            Register
          </Button>
          <Button variant="secondary" href="/login">
            Login
          </Button>
          <Button variant="tertiary" href="/generate">
            experiment🪄
          </Button>
        </div>
      </div>
    </>
  );
}
