03.02.2025
- created basic structure for the backend
- token implementation for secure access added into the backend
- created schema for story and user models

04.02.2025
- changed the schema for user and story till it was accepted
- added routes to use crud operations on user and story
- changed the jsonwebtoken to work with an authorization header
- tried bruno (https://www.usebruno.com/) as an api testing which won't need a login
- changing the technical documentary for the api

05.02.2025
- discussions about the name of the site, github issues, design
- added routes for contents (external apis in this case bedrock with an example implementation)
- logging was implemented as middleware. at first the logs contained the tokens in clear text so a filter was implemented. logging was added to the routes

info: apireq caching/saving in nodecache, redis or mongodb?

